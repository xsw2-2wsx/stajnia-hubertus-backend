summary: Operations on user proile picture
post:
  tags:
    - profile

  requestBody:
    $ref: "../requestBodies/ImageUploadRequestBody.yaml"

  responses:
    '200':
      description: Operation successfull
    default:
      $ref: "../responses/ApiErrorResponse.yaml"
