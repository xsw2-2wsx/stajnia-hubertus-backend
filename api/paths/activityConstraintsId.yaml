summary: Operations on the activity with specified id
parameters:
  - name: activityConstraintId
    in: path
    required: true
    schema:
      $ref: "../schemas/Id.yaml"

get:
  tags:
    - activity constraints

  responses:
    '200':
      $ref: "../responses/ActivityConstraintResponse.yaml"
    default:
      $ref: "../responses/ApiErrorResponse.yaml"

delete:
  tags:
    - activity constraints
  description: Deletes specified activity

  responses:
    '200':
      description: Operation sucessfull
    default:
      $ref: "../responses/ApiErrorResponse.yaml"
