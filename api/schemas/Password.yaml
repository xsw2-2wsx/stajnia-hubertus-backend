type: string
pattern: ^(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,20}$
x-pattern-error: Hasło musi mieć długość od 8 do 20 znaków oraz zawierać co najmniej jedną małą literę, jedną duża literę
  oraz jedną cyfrę